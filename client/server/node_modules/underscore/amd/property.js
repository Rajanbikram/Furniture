define(['./_deepGet', './_toPath'], function (_deepGet, _toPath) {
  function property(path) {
    path = _toPath(path);
    return function(obj) {
      return _deepGet(obj, path);
    };
  }
  return property;
});
